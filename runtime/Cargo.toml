[package]
name = "q-reg-runtime"
version = "0.1.1"
edition = "2021"
authors = ["Jacarri Sanders <Even The Odds Foundry>"]
description = "Thin deterministic runtime host for Q-REG (formal proofs in ../formal). Pure gate classification surface; gRPC planned."
license = "MIT"
repository = "https://github.com/jabrahns-source/Q-Reg"

[dependencies]
# Future: tonic = "0.12", tokio = { version = "1", features = ["full"] }, prost = "0.13"
# Current: pure std classification for determinism parity with Python engine.

[profile.release]
lto = true
codegen-units = 1
